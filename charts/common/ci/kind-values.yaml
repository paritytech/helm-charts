image:
  repository: nginx
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  annotations: {}
  type: ClusterIP
  ports:
  - name: http
    port: 80
    containerPort: 80
    protocol: TCP


config:
  test.yaml: |-
    test: data