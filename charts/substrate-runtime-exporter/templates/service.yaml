apiVersion: v1
kind: Service
metadata:
  name: {{ include "substrate-runtime-exporter.fullname" . }}
  labels:
    {{- include "substrate-runtime-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "substrate-runtime-exporter.selectorLabels" . | nindent 4 }}
